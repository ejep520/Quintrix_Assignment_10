debug: true

tenth:
  srvhost: DESKTOP-4NACQ64
  
spring:
  application:
    name: LoginService
  datasource:
    url: jdbc:h2:mem:tenth
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: ${{secrets.Tenth_ClientId}}
            clientSecret: ${{secrets.Tenth_ClientSecret}}
  cloud:
    inetutils:
      preferredNetworks: 192.168.1,96.39
    client:
      hostname: ${tenth.srvhost}

eureka:
  client:
    serviceUrl:
      defaultZone: http://${tenth.srvhost}:8761/eureka
    instance:
      hostname: ${tenth.srvhost}
      preferIpAddress: true
